#!/usr/bin/env bash
set -euo pipefail

if (( $# != 1 )); then
  echo 'usage: workloads/posthog <source-directory>' >&2
  exit 2
fi

readonly source_directory=$1
exec docker buildx build \
  --platform linux/amd64,linux/arm64 \
  --build-arg BUILDKIT_CONTEXT_KEEP_GIT_DIR=1 \
  "$source_directory"
